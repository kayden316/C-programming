familirazation wit c-programming
1 WAP to print "HELLO WORLD" on screen 
2  WAP to take two integer number as input from the user and show the sum of the number as result
3 Define PI as the symbolic constant and take radius as input from the user to calculate the area of circle
4 take input and store in integer variavle "a" and assign a value to integer "x" after both "increment prefix" 
and "increment portfix"and print the value of "A" and "n" for both case. 